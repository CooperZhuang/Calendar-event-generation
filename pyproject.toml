[project]
name = "calendar-event-generation"
version = "1.3.0"
description = "AI 日程解析 → .ics 生成 + 自动插入 Mac 日历（图片/文本识图、去重、自动发现新地点）"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = []

[project.urls]
Homepage = "https://github.com/CooperZhuang/calendar-event-generation"
